import { Card, CardBody } from "@nextui-org/react";

export const Placeholder = () => {
  return (
    <Card className="h-full border border-divider">
      <CardBody>No data, please select a request file...</CardBody>
    </Card>
  );
};
